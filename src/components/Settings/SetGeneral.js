import React from "react";
import withContext from "../../withContext";

const SetGeneral = (props) => {

    return (
        <div className="hero">
            <h1> Set General </h1>
        </div>
    )
}

export default withContext(SetGeneral);