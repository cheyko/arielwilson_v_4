import React from "react";
import withContext from "../../withContext";

const SetSafety = props => {
    return(
        <div className="hero">
            <h1> Set Safety</h1>
        </div>
    )
}

export default withContext(SetSafety);