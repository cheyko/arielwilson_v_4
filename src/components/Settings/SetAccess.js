import React from "react";
import withContext from "../../withContext";

const SetAccess = props => {
    return(
        <div className="hero">
            <h1> Set Access </h1>
        </div>
    )
}

export default withContext(SetAccess);