import React from "react";
import withContext from "../../withContext";

const SetAccount = (props) => {

    return (
        <div className="hero">
            <h1> Set Account </h1>
        </div>
    )
}

export default withContext(SetAccount);