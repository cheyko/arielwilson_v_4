import React from "react";
import withContext from "../../withContext";

const SetNotification = (props) => {

    return (
        <div className="hero">
            <h1> Set Notification </h1>
        </div>
    )
}

export default withContext(SetNotification);