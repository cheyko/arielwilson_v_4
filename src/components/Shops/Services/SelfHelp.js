import React from "react";
import withContext from "../../../withContext";

const SelfHelp = props => {

    return (
        <div className="hero sub-container">
            <div className="hero-body">
                <h1>Self Help</h1>
            </div>
        </div>
    )
}

export default withContext(SelfHelp);