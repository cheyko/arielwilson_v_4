import React from "react";
import withContext from "../../../withContext";

const Operator = props => {

    return(
        <div className="hero sub-container">
            <div className="hero-body">
                <h1>Operator</h1>
            </div>
        </div>
    )
}
export default withContext(Operator);