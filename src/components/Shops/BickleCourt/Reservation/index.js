import React from "react";
import withContext from "../../../../withContext";

const Reservations = props => {

    return(
        <div className="hero sub-container">
            <div className="hero-body">
                <h1>Reservations</h1>
            </div>
        </div>
    )
}
export default withContext(Reservations);