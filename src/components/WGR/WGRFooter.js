import React from "react";
import withContext from "../../withContext";

const WGRFooter = props => {
    return (
        <>
            <div className="hero">
                <div className="box">
                    <h1>WGR FOOTER</h1>
                </div>
            </div>
        </>    
    );
}

export default withContext(WGRFooter);