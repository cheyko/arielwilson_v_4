import React from "react";
import withContext from "../../withContext";
import "./index.css";

const Talkie = props => {

    return(
        <div className="hero">
            <div className="hero-body">
                <h1>Voice Over Internet Protocol </h1>
            </div>

        </div>
    )
}
export default withContext(Talkie);